# Hohenzollern 城堡复审（2026-09-21）

照片级连续验收仍为 **0**，没有安装全景或修改 `assets/life/panoramas/manifest.json`。

## 作业与版本范围

- A800 作业：`167855`，分区 `hp_a800`，节点 `d1n41a18g03`，`COMPLETED`，耗时 `02:30`。
- 首次作业 `167851` 因 Blender 三角剖分 API 差异在 `00:07` 失败，修正后重跑为 `167855`。失败日志保留，但失败源码已被重跑覆盖，不能称为完整失败快照。
- 来源：LZCreation Hohenzollern Castle，CC BY 4.0；461,176 三角面、16K 单漫反射图。
- 研究目录：`.render-work/public-castle-hohenzollern-20260921-r7/`，本次图像来自其中 `renders/` 的 `167855` 产物。
- `167855` 渲染的是当时用 bisect 切割边界闭环封口、沿边界向下延伸岩裙的版本。当前 `scripts/filter-hohenzollern-skirt.py` 已改为边界诊断工具；主代理报告 `167947` 的 12 项几何回归通过，但当前代码尚无新渲染。本次旧图不能证明当前代码的视觉效果，也不能据此宣称黑缝根因已经全部解决。

## 独立审图身份与范围

本次审图人为子代理 `scan_quality_audit`，未参与此版本实现或渲染，独立逐张查看了 `.render-work/review-20260921/castle-{clear,dusk}-{six,close}.jpg` 共四张 contact。原文初稿由主代理写成，本次新增的独立结论仅针对这些实际查看的图像。

`six` 的 north、east、south、west、elevated、underside 来自两相原生多机位透视渲染，`close` 为两相四个原生立面近景，共覆盖 20 个原生视图的缩略图；它们不是固定观察点全景的六向投影。已阅读生成脚本及 `renders/evidence.json`、`verification.json`，后者记录原图哈希与 1600×1100 尺寸一致。本次视觉审查使用 contact 缩图，未逐一检查原 PNG 全分辨率，也未验证浏览器交互或完整 360° 场景；缩图中已有足够明确的否决问题。

## 独立结论

晴天、黄昏均判定 **FAIL**，不能计入连续通过轮数。

- 四个立面近景均能辨认成组尖塔、石色墙面和宫殿式轮廓，符合用户对漂亮尖塔城堡的形体方向；这只支持继续研究建筑主体，不代表整个来源已达照片级。
- near-north-facade 与 near-east-facade 的前景出现大块绿色、黄绿色平滑三角片，树冠与坡面融成拉扯的皱褶，局部黑洞和断边清晰可见。它们既不像独立树木，也不像自然草地或岩壁；黄昏仍保留同样问题。
- north、east 的植被底边形成醒目的水平硬切带，直接接到纵向拉伸的岩裙；south、west 的岩裙像竖向折叠帘，边缘出现长尖片与不规则齿状底沿。加入真实岩石颜色没有使形体成为可信山崖。
- elevated 与 underside 清楚呈现孤立截断地块和大块平直底面，周围没有接续山坡或山谷环境。当前图可用于来源诊断，不能代表自然地形中完整落地的城堡场景。

该版本不得安装。下一轮应先证明建筑与融合植被能够可靠分离，并以自然地形取代硬切环和拉伸岩裙；若无法保留完整城墙、立面及尖塔，应更换具有可用侧面与独立地形的宫殿摄影测量源。必须对实际将使用的代码重新渲染并审查，几何回归或高面数不能替代照片级验收。
